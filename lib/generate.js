const path = require('path');
const fs = require('fs');

const directoryHTML = path.resolve(__dirname, "../templates");

const generate = team => {
    const html = [];
}













module.exports = generate;